import { createClient } from "@supabase/supabase-js";
const [,,adminPassword,userPassword]=process.argv;
if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.SUPABASE_SERVICE_ROLE_KEY||!adminPassword||!userPassword){console.error("Usage: NEXT_PUBLIC_SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... node scripts/create-demo-users.mjs <admin-password> <user-password>");process.exit(1)}
const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}});
for(const u of [{email:"admin@khairatabad.demo",password:adminPassword,full_name:"Demo Administrator",username:"demo_admin",mobile:"9876543210",role:"admin"},{email:"devotee@khairatabad.demo",password:userPassword,full_name:"Demo Devotee",username:"demo_user",mobile:"9123456789",role:"user"}]){const {data,error}=await s.auth.admin.createUser({email:u.email,password:u.password,email_confirm:true});if(error){console.error(u.username,error.message);continue}const {error:pe}=await s.from("profiles").insert({user_id:data.user.id,full_name:u.full_name,username:u.username,mobile:u.mobile,email:u.email,role:u.role});console.log(pe?`${u.username}: ${pe.message}`:`Created ${u.username}`)}
