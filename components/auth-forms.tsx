"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link"; import { Alert } from "./ui";
export function LoginForm(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget);const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(fd))});const body=await res.json();if(!res.ok){setError(body.error);setBusy(false);return}router.push(body.redirect);router.refresh()}return <form onSubmit={submit} className="space-y-5">{error&&<Alert>{error}</Alert>}<div><label className="label" htmlFor="username">Username</label><input className="input" id="username" name="username" autoComplete="username" required/></div><div><label className="label" htmlFor="password">Password</label><input className="input" id="password" name="password" type="password" autoComplete="current-password" required/></div><button disabled={busy} className="btn btn-primary w-full">{busy?"Signing in…":"Sign in"}</button><p className="text-center text-sm text-stone-500">New devotee? <Link className="font-bold text-orange-700" href="/register">Register</Link></p></form>}
export function RegisterForm(){const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget);const res=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(fd))});const body=await res.json();if(!res.ok){setError(body.error);setBusy(false);return}router.push("/login?registered=1")}const fields=[["fullName","Full name","text","name"],["username","Username","text","username"],["mobile","Mobile number","tel","tel"],["email","Email address","email","email"],["password","Password","password","new-password"],["confirmPassword","Confirm password","password","new-password"]];return <form onSubmit={submit}><div className="grid gap-5 sm:grid-cols-2">{fields.map(([n,l,t,a])=><div key={n}><label className="label" htmlFor={n}>{l}</label><input className="input" id={n} name={n} type={t} autoComplete={a} required minLength={n==="password"?8:undefined}/></div>)}</div>{error&&<div className="mt-5"><Alert>{error}</Alert></div>}<p className="mt-5 text-xs text-stone-500">Password must be at least 8 characters and include uppercase, lowercase, number and special character.</p><button disabled={busy} className="btn btn-primary mt-6 w-full">{busy?"Creating account…":"Register account"}</button></form>}
