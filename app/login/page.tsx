import { LoginForm } from "@/components/auth-forms"; import { getSessionProfile } from "@/lib/auth"; import { redirect } from "next/navigation";
export default async function Login(){const p=await getSessionProfile();if(p)redirect(p.role==="admin"?"/admin":"/dashboard");return <AuthShell title="Welcome back" subtitle="Sign in with your registered username"><LoginForm/></AuthShell>}
function AuthShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <div className="shell grid min-h-[70vh] place-items-center py-12"><div className="card w-full max-w-md p-7 sm:p-9"><div className="mb-7 text-center"><div className="mx-auto mb-3 grid h-14 w-14 place-items-center rounded-full bg-orange-100 text-3xl text-orange-700">ॐ</div><h1 className="text-2xl font-black text-orange-950">{title}</h1><p className="mt-2 text-sm text-stone-500">{subtitle}</p></div>{children}</div></div>}
