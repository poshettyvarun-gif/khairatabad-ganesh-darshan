import { requireUser } from "@/lib/auth";import { createAdminClient } from "@/lib/supabase/admin";import { DashboardNav } from "@/components/dashboard-nav";
export default async function Users(){await requireUser("admin");const a=createAdminClient(),{data}=await a.from("profiles").select("*,bookings(count)").order("created_at",{ascending:false});return <div className="shell py-8"><DashboardNav admin/><h1 className="text-3xl font-black text-orange-950">Registered users</h1><p className="mt-2 mb-7 text-stone-500">Read-only directory of registered devotees and administrators.</p><div className="card table-wrap"><table className="table"><thead><tr><th>Name</th><th>Username</th><th>Mobile</th><th>Email</th><th>Role</th><th>Joined</th></tr></thead><tbody>{data?.map(p=><tr key={p.id}><td className="font-bold">{p.full_name}</td><td>@{p.username}</td><td>{p.mobile}</td><td>{p.email}</td><td><span className="badge badge-pending">{p.role}</span></td><td>{new Date(p.created_at).toLocaleDateString("en-IN")}</td></tr>)}</tbody></table></div></div>}
