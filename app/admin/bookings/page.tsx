import { requireUser } from "@/lib/auth";import { createAdminClient } from "@/lib/supabase/admin";import { DashboardNav } from "@/components/dashboard-nav";import { AdminBookings } from "@/components/admin-bookings";import Link from "next/link";
export default async function Page(){await requireUser("admin");const a=createAdminClient();const {data}=await a.from("bookings").select("*,profiles!bookings_user_id_fkey(*),time_slots(*,darshan_dates(date,is_active))").order("created_at",{ascending:false});return <div className="shell py-8"><DashboardNav admin/><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-3xl font-black text-orange-950">Booking requests</h1><p className="mt-2 text-stone-500">Review, filter and manage all requests.</p></div><Link className="btn btn-secondary" href="/api/admin/bookings/export">Export all CSV</Link></div><AdminBookings initial={data??[]}/></div>}
