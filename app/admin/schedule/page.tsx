import { requireUser } from "@/lib/auth";import { createAdminClient } from "@/lib/supabase/admin";import { DashboardNav } from "@/components/dashboard-nav";import { ScheduleManager } from "@/components/schedule-manager";
export default async function Schedule(){await requireUser("admin");const a=createAdminClient(),{data}=await a.from("darshan_dates").select("*,time_slots(*)").order("date");return <div className="shell py-8"><DashboardNav admin/><h1 className="text-3xl font-black text-orange-950">Darshan schedule management</h1><p className="mt-2 mb-7 text-stone-500">Create dates and slots, control availability and monitor capacity.</p><ScheduleManager dates={data??[]}/></div>}
