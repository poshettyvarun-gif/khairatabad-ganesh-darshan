import { requireUser } from "@/lib/auth";import { DashboardNav } from "@/components/dashboard-nav";import { BookingWizard } from "@/components/booking-wizard";
export default async function BookingPage(){const p=await requireUser("user");return <div className="shell py-8"><DashboardNav/><div className="mx-auto max-w-4xl"><p className="text-sm font-bold text-orange-700">NEW REQUEST</p><h1 className="text-3xl font-black text-orange-950">Book your Darshan</h1><p className="mt-2 mb-7 text-stone-500">Select an active date and available time slot. Requests require admin approval.</p><BookingWizard profile={{full_name:p.full_name,mobile:p.mobile}}/></div></div>}
